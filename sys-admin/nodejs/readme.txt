-------- windows
1. download and setup node-v0.10.1-x64.msi

--------- linux
	$ apt-get install nodejs 
		or
	$ yum install nodejs


2. using npm (node package manager )  you can install any
	> npm install <packagename>
2.1 to install a package to the system scope ( global install )
	> npm install -g <packagename>
	( modules goes to <install dir>/lib/node_modules
		or /usr/local/lib/node_modules etc

3. sometimes Python required as a dependency, so it's better install python too
		python-2.7.2.msi